现在有一个草案，这是一个不了解咱们项目UI的人给出的建议，你需要结合目前的UI设计现状，适当修改这个草案，不要修改代码！
## 一、核心设计系统（全端统一）
### 1.1 基础规范
| 类别 | 规则 |
|------|------|
| 主色调 | 微信绿 #07C160（主按钮、选中态、成功态）<br>Telegram蓝 #2AABEE（链接、次要强调） |
| 中性色 | 背景 #F5F5F5 / #FFFFFF<br>文字 #111111（主）/ #666666（次）/ #999999（辅助）<br>分割线 #E5E5E5 |
| 圆角 | 8px（卡片、气泡）/ 12px（弹窗、面板）/ 50%（头像） |
| 阴影 | 轻阴影 `0 2px 8px rgba(0,0,0,0.08)`（悬浮元素）<br>中阴影 `0 4px 16px rgba(0,0,0,0.12)`（弹窗） |
| 字体 | 系统无衬线字体（Windows: Segoe UI / Mac: SF Pro / 移动端: 系统默认）<br>字号：12px（辅助）/ 14px（正文）/ 16px（标题）/ 18px（大标题） |
| 响应式断点 | PC端：≥1200px<br>平板端：768px-1199px<br>手机端：<768px |

### 1.2 通用组件规范
- **按钮**：主按钮（绿底白字，圆角8px）/ 次按钮（灰底黑字）/ 文字按钮（无背景，蓝色/绿色文字）
- **开关**：绿色开启（#07C160）/ 灰色关闭（#CCCCCC）
- **角标**：红色圆形（未读消息）/ 蓝色圆形（新功能）/ 黄色徽章（管理员）
- **输入框**：浅灰背景 #F5F5F5，圆角8px，聚焦时边框变主色调

## 二、PC端完整优化（≥1200px）
### 2.1 整体三栏布局
```
[一级侧边栏 64px 固定] | [二级侧边栏 320px 固定] | [内容区 弹性 占满剩余]
```
- 一级侧边栏：纯图标导航，选中态绿色背景+白色图标
- 二级侧边栏：白色背景，右侧1px浅灰分割线
- 内容区：浅灰背景 #F5F5F5

### 2.2 一级侧边栏（优化）
- 图标：线性图标，未选中灰色，选中白色+绿色背景
- 顺序：首页（消息）、通讯录、广场（待开发，灰色禁用）、设置
- 底部：用户头像（点击展开个人快捷菜单：切换状态、退出登录）
- 未读角标：首页图标右上角显示总未读消息数

### 2.3 首页（消息）模块（完整优化）
#### 2.3.1 二级侧边栏（消息列表）
- 顶部搜索框：支持搜索消息内容、联系人、群聊
- 搜索框下方：「置顶消息」分组（折叠/展开），然后是普通消息列表
- 消息列表项（微信+Telegram融合）：
  - 左侧：圆形头像（群聊显示群头像，个人显示个人头像）
  - 中间上：昵称/群名（加粗）+ 未读消息角标（右上角）
  - 中间下：最新消息摘要（灰色，单行省略）+ 时间戳（最右侧）
  - 选中态：浅绿背景 #E8F8EF + 左侧3px绿色竖线
- 右键菜单（保留原功能，优化样式）：
  - 置顶聊天/取消置顶
  - 标记为未读/已读
  - 更改备注
  - 从列表删除
  - 退出群聊（仅群聊）

#### 2.3.2 内容区（聊天窗口）
- **顶部Header**（固定高度56px）：
  - 左侧：群名/昵称 + 在线状态（绿色小点）
  - 右侧：搜索图标 + 三点设置图标
  - 底部1px浅灰分割线
- **消息区域**（弹性滚动）：
  - 消息气泡：
    - 对方消息：白色背景，左对齐，圆角8px（左上角直角）
    - 自己消息：绿色背景 #DCFCE7，右对齐，圆角8px（右上角直角）
    - 时间戳：气泡下方，灰色小字，居中显示（间隔5分钟以上显示）
  - 已读状态：自己消息右下角显示「已读」（灰色）/ 「送达」（灰色）
  - 消息右键菜单（保留原功能，优化分组）：
    - 基础组：复制、转发、引用、多选
    - 管理组：撤回（2分钟内）、删除（本地）、置顶（仅群主）
- **底部输入区**（固定高度64px）：
  - 左侧：表情按钮 + 文件上传按钮 + 语音按钮
  - 中间：多行输入框（支持自动换行，最大高度200px）
  - 右侧：发送按钮（绿色，输入内容后显示）
- **群设置面板**（从右向左滑出，宽度360px，占满高度）：
  完全保留原6模块结构，优化样式：
  - 群信息头部：大头像 + 群名 + 群人数 + 群描述
  - 群公告：可折叠，编辑后自动同步到聊天区顶部
  - 群成员列表：每行4个头像，点击头像查看个人资料
  - 快捷功能：查找聊天记录、群文件、群相册
  - 聊天设置：消息免打扰、置顶聊天、显示群成员昵称、屏蔽群消息
  - 底部危险操作：退出群聊、解散群聊（仅群主）

### 2.4 通讯录模块（完整优化）
#### 2.4.1 二级侧边栏
- 顶部搜索框：支持搜索好友、群聊
- 搜索框下方：横向标签栏（「好友」「群聊」「新朋友」）
- 标签栏下方：
  - 好友列表：按拼音首字母分组，右侧悬浮字母索引条
  - 群聊列表：按最近联系时间排序
  - 新朋友列表：显示未处理申请，带红色角标
- 底部：「添加好友」按钮（绿色文字）

#### 2.4.2 内容区
- **默认状态**：居中占位「选择一个联系人查看详情」
- **新朋友详情**：
  - 顶部：「新的朋友」标题
  - 列表项：头像 + 昵称 + 申请理由 + 「接受」「拒绝」按钮
  - 接受后弹出「添加备注」弹窗
- **好友详情**：
  - 顶部：大头像 + 昵称 + 备注（可编辑）
  - 信息区：ID（可复制）、手机号（可选）、个性签名
  - 快捷操作：发消息、语音通话、视频通话
  - 更多操作：修改备注、删除好友、加入黑名单
- **群聊详情**：
  直接复用首页群设置面板的完整布局

### 2.5 设置模块（完整优化）
#### 2.5.1 二级侧边栏
- 顶部：「设置」标题（18px加粗）
- 导航列表（分段+分隔线）：
  - 个人信息
  - 通知设置
  - 聊天设置
  - 隐私设置
  - 存储空间
  - 身份与安全
  - 关于我们

#### 2.5.2 内容区（各子页面）
- **个人信息**：
  - 头像（可更换）、昵称（可编辑）、个性签名（可编辑）
  - 我的二维码（点击放大，可分享）
  - 设备指纹、用户ID（均可复制）
- **通知设置**：
  - 消息提示音（开关+选择器）
  - 语音通话提示音（开关+选择器）
  - 浏览器通知（开关+权限状态）
  - 显示消息预览（开关）
- **聊天设置**：
  - 字体大小（滑块调节+预览）
  - 深色模式（开关）
  - 回车键发送消息（开关）
  - 自动下载图片/视频（开关）
- **隐私设置**：
  - 谁可以加我为好友（所有人/仅好友的好友/没人）
  - 谁可以看到我的在线状态（所有人/仅好友/没人）
  - 黑名单列表
- **存储空间**：
  - 已用空间进度条 + 分类占比（图片/视频/文件/缓存）
  - 清理聊天记录（按时间范围选择）
  - 清理缓存数据
- **身份与安全**：
  - 导出助记词（需验证密码）
  - 导入助记词
  - 修改密码
- **关于我们**：
  - 版本号、检查更新
  - 用户协议、隐私政策
  - 调试模式（开关，开启后显示调试日志）

## 三、平板端响应式适配（768px-1199px）
### 3.1 布局切换逻辑
采用**两栏布局**，隐藏一级侧边栏，通过汉堡菜单呼出：
```
[汉堡菜单按钮] | [二级侧边栏 280px 固定] | [内容区 弹性]
```
- 点击左上角汉堡菜单，一级侧边栏从左向右滑出（宽度240px）
- 一级侧边栏滑出时，内容区变暗遮罩，点击遮罩关闭侧边栏

### 3.2 适配优化
- 二级侧边栏宽度从320px缩减到280px
- 消息列表项间距适当缩小
- 群设置面板宽度从360px缩减到320px
- 输入区高度保持64px，按钮尺寸不变
- 通讯录字母索引条宽度缩小，点击区域放大

### 3.3 特殊交互
- 横屏时保持两栏布局
- 竖屏时自动切换为手机端单栏布局（见下文）
- 支持手势滑动：从左边缘向右滑呼出一级侧边栏，从右边缘向左滑关闭群设置面板

## 四、手机端响应式适配（<768px）
### 4.1 布局切换逻辑
采用**单页面切换**布局，三个区域分别作为独立页面，通过底部导航栏切换：
```
[顶部状态栏]
[内容区 占满全屏]
[底部导航栏 固定高度56px]
```
- 底部导航栏：首页（消息）、通讯录、设置（广场待开发，隐藏）
- 所有二级页面通过「返回按钮」从左向右滑入

### 4.2 首页（消息）页面
- 顶部：搜索框 + 右上角「+」按钮（添加好友、发起群聊）
- 消息列表：同PC端样式，优化为移动端尺寸
- 滑动操作：左滑消息项显示「置顶」「删除」按钮
- 聊天窗口：
  - 顶部：返回按钮 + 群名/昵称 + 右上角三点设置
  - 消息区域：同PC端，气泡尺寸适当缩小
  - 底部输入区：高度56px，表情、语音、发送按钮尺寸适配
- 群设置：作为独立页面，从右向左滑入，顶部有返回按钮

### 4.3 通讯录页面
- 顶部：搜索框 + 右上角「添加」按钮
- 标签栏：「好友」「群聊」「新朋友」
- 好友列表：按拼音首字母分组，右侧字母索引条
- 联系人详情：作为独立页面，顶部返回按钮 + 右上角「发消息」按钮

### 4.4 设置页面
- 顶部：「设置」标题
- 设置列表：同PC端，优化为移动端列表样式
- 每个设置项作为独立子页面，点击进入

### 4.5 移动端专属优化
- 所有按钮点击区域≥48x48px
- 支持下拉刷新消息列表
- 支持长按消息弹出菜单
- 输入框弹出时，页面自动上移，避免被键盘遮挡
- 适配刘海屏、挖孔屏，顶部状态栏留出安全距离

## 五、通用交互与边界规则
1. **动画过渡**：所有页面切换、面板滑出使用300ms缓动动画
2. **加载状态**：数据加载时显示骨架屏，避免空白
3. **错误处理**：网络错误时显示重试按钮，操作失败时显示Toast提示
4. **深色模式**：全端支持深色模式，自动跟随系统设置
5. **离线状态**：支持离线查看聊天记录，发送消息缓存到本地，联网后自动重发



